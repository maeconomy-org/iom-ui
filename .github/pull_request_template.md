<!-- PRs target `dev`. We merge with a merge commit (not squash) — keep commits reviewable. -->

## Summary

<!-- What this PR does and why, in a few sentences. Lead with the outcome (the capability or
     fix delivered), then the reason. Link any related issue. -->

## Type of change

<!-- Keep the ones that apply. -->

- Feature
- Fix
- Refactor
- Test
- CI / tooling
- Docs

## Changes

<!-- The notable changes — ideally one bullet per commit, in order, so the PR reads
     top-to-bottom. Call out anything non-obvious or that shaped the design. -->

-

## How it was verified

<!-- Tests added/updated, manual steps run, API requests exercised, `pnpm verify` result.
     Enough that a reviewer can trust it without re-deriving it. -->

-

## Breaking changes / migration

<!-- API, schema, config, or behaviour changes and what a consumer must do about them.
     Write "None" if there are none. -->

None

## Checklist

- [ ] `pnpm fullcheck` green locally (typecheck / lint / format / test / build)
- [ ] Tests cover the change (or a note on why they don't)
- [ ] Docs / comments updated if behaviour changed
- [ ] Commits are phased and individually reviewable
- [ ] No secrets, keys, or `.env` values committed

## Notes for the reviewer

<!-- What to look at first, known risks or trade-offs, and follow-ups deliberately left out
     of scope (with a pointer to where they'll be handled). -->
