# UI Local Development Environment Variables
# Copy this file to .env.local for local development
# All variables are runtime-configurable (no rebuild needed)

# =============================================================================
# REQUIRED VARIABLES (must be configured)
# =============================================================================

# Environment Configuration
NODE_ENV="development"

# API Configuration (REQUIRED)
BASE_API_URL="https://your-api.example.com:9443"
UUID_API_URL="https://your-api.example.com:8443"

# Certificate Configuration (REQUIRED for mTLS)
CERTIFICATE_PATH="./certs/your-certificate.pem"
CERTIFICATE_PASSWORD="your-certificate-password"

# HERE Maps API Key (REQUIRED for map features)
HERE_API_KEY=your-here-api-key

# Redis Configuration (REQUIRED - use local Redis)
REDIS_URL="redis://localhost:6379"
REDIS_PASSWORD=your-secure-redis-password

# =============================================================================
# DEVELOPMENT DEFAULTS (recommended for local dev)
# =============================================================================

# Security & TLS (relaxed for development)
VERIFY_CERTIFICATES=false
ALLOW_INSECURE_FALLBACK=true

# App Branding (optional customization)
# APP_NAME="Internet of Materials (Dev)"
# APP_DESCRIPTION="Material Management System - Development"
# APP_ACRONYM="IoM-Dev"
# SUPPORT_EMAIL="dev-support@internetofmaterials.com"

# Import Limits (generous for development)
# MAX_FILE_SIZE_MB=100
# MAX_IMPORT_PAYLOAD_MB=100
# MAX_OBJECTS_PER_IMPORT=50000
# MAX_CONCURRENT_JOBS_PER_USER=10

# Import Performance (development-friendly)
# API_BATCH_SIZE=25
# PARALLEL_REQUESTS=10                # Lower for development
# API_REQUEST_DELAY=100               # Slower for debugging

# Rate Limiting (relaxed for development)
# RATE_LIMIT_WINDOW_MINUTES=10
# RATE_LIMIT_MAX_REQUESTS=50          # Higher limit for dev
# RATE_LIMIT_WARNING_THRESHOLD=40

# Redis Configuration (local development)
# REDIS_JOB_TTL_HOURS=24
# REDIS_CHUNK_TTL_HOURS=6

# Logging (simplified - only LOG_LEVEL needed)
LOG_LEVEL=debug                       # debug, info, warn, error

# Health UI (enabled for development)
# HEALTH_UI_ENABLED=true
# HEALTH_ALLOWED_CERTS=               # Comma-separated cert fingerprints/serials for access

# Sentry Configuration (optional for development)
# SENTRY_DSN=https://your-key@o123.ingest.de.sentry.io/456
# SENTRY_ORG=your-organization-slug
# SENTRY_PROJECT=your-project-name-dev
# SENTRY_ENABLED=false
# SENTRY_RELEASE=dev

# =============================================================================
# DEVELOPMENT TIPS
# =============================================================================
# For local development:
# 1. Set NODE_ENV=development for better error messages
# 2. Enable LOG_LEVEL=debug for detailed logging
# 3. Use HEALTH_UI_ENABLED=true to monitor system health
# 4. Set VERIFY_CERTIFICATES=false for self-signed certs
# 5. Use higher RATE_LIMIT_MAX_REQUESTS for testing
#
# For production testing:
# 1. Set NODE_ENV=production
# 2. Enable SENTRY_ENABLED=true
# 3. Use stricter rate limits
# 4. Enable certificate verification if possible